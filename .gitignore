node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local waitlist fallback storage — contains signup emails
data/

# Exported waitlist CSVs — real people's email addresses, never commit these
backups/
